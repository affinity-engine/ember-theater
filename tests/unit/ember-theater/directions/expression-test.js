import expression from 'ember-theater/ember-theater/directions/expression';
import { module, test } from 'qunit';

module('Unit | EmberTheaterDirection | expression');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = expression();
  assert.ok(result);
});
