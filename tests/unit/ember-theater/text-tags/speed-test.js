import speed from 'ember-theater/ember-theater/text-tags/speed';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | speed');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = speed();
  assert.ok(result);
});
