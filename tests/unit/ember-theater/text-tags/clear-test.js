import clear from 'ember-theater/ember-theater/text-tags/clear';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | clear');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = clear();
  assert.ok(result);
});
