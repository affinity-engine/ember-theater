import pause from 'ember-theater/ember-theater/text-tags/pause';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | pause');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pause();
  assert.ok(result);
});
