import close from 'ember-theater/ember-theater/text-tags/close';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | close');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = close();
  assert.ok(result);
});
