import effect from 'ember-theater/ember-theater/text-tags/effect';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | effect');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = effect();
  assert.ok(result);
});
