import instant from 'ember-theater/ember-theater/text-tags/instant';
import { module, test } from 'qunit';

module('Unit | EmberTheaterTextTag | instant');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = instant();
  assert.ok(result);
});
