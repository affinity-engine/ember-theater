import gatherModules from '../../../utils/gather-modules';
import { module, test } from 'qunit';

module('Unit | Utility | gather modules');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gatherModules();
  assert.ok(result);
});
