import gatherModuleNames from '../../../utils/gather-module-names';
import { module, test } from 'qunit';

module('Unit | Utility | gather module names');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gatherModuleNames();
  assert.ok(result);
});
