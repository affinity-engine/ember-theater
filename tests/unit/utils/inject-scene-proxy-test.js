import injectSceneProxy from '../../../utils/inject-scene-proxy';
import { module, test } from 'qunit';

module('Unit | Utility | inject scene proxy');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = injectSceneProxy();
  assert.ok(result);
});
