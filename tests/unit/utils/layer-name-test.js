import layerName from '../../../utils/layer-name';
import { module, test } from 'qunit';

module('Unit | Utility | layer name');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = layerName();
  assert.ok(result);
});
