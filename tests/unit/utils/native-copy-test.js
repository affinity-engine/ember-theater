import nativeCopy from '../../../utils/native-copy';
import { module, test } from 'qunit';

module('Unit | Utility | native copy');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = nativeCopy();
  assert.ok(result);
});
