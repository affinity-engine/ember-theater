import { emberTheaterJoinedArray } from 'dummy/helpers/ember-theater/joined-array';
import { module, test } from 'qunit';

module('Unit | Helper | ember theater/joined array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberTheaterJoinedArray([42]);
  assert.ok(result);
});
