{{#if mediaIsLoaded}}
  {{#each-in emberTheaterComponents as |componentName options|}}
    {{component componentName options=options}}
  {{/each-in}}
{{else}}
  {{ember-theater/curtain complete=(action 'startGame')}}
{{/if}}
